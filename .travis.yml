language: java

sudo: required

services:
  - docker

env:
  ACTIVEMQ_VERSION: 5.14.3
  CAMEL_VERSION: 2.16.3

before_install:
  - chmod +x /app/run.sh
  - sudo mv docker-compose /usr/local/bin


script:
  - echo "Hello World"

